import ProductList from "./ProductList";

const BudgetCakes = () => {
  // const filterBudget = productItems.filter((item) => item.category === 'budget')
  // console.log(filterBudget);

  return <ProductList category="budget" />;
};
export default BudgetCakes;
